flutter_gen:
  output: lib/gen/ # Optional (default: lib/gen/)
  line_length: 80 # Optional (default: 80)

  integrations:
    flutter_svg: true
    lottie: true

  assets:
    enabled: true

  images:
    enabled: true # Automatically generates code for image assets.

  fonts:
    enabled: true